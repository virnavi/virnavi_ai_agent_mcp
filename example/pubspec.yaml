name: virnavi_ai_agent_mcp_example
description: "Example Flutter app showing annotation-driven MCP tool registration."
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.9.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  virnavi_ai_agent_mcp:
    path: ../
  virnavi_ai_agent_compose:
    path: ../compose/

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.8.0
  virnavi_ai_agent_mcp_generator:
    path: ../generator/
